import { Container } from "./styles";

function PageCard({ backgroundColor }: { backgroundColor: string }) {
  return <Container backgroundColor={backgroundColor}></Container>;
}

export default PageCard;
